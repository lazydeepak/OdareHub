# Services

Placeholder for future Customization Studio services.

Services added later must preserve Studio as a governed tool area, not runtime truth. Platform/System owns approved active style registry truth, Shell owns style sockets and runtime consumption, apps/modules own scoped CSS and consume sockets, organization owns brand identity, and `public/assets` remains generated delivery output only.
