# OdareHub Backlog

This file tracks confirmed future work that is not the current active task. Keep speculative ideas out unless they have a clear owner or reason.

## Product Roadmap

From `README.md` short-term roadmap:

- Finalize widget-based dashboard system.
- Remove cockpit-style pages.
- Normalize routing between `/manufacturing` and `/apps/manufacturing`.
- Clean `/me` into a true command center.
- Build worker-perspective UI surfaces.

From `README.md` mid-term roadmap:

- App packaging through ZIP install/export.
- Migration runner and schema sync improvements.
- Plugin widget registry.
- API layer for integrations.

From `README.md` long-term roadmap:

- Marketplace for apps/plugins.
- Multi-tenant deployment support.
- Advanced analytics and forecasting.
- Mobile-first dashboards.

## Documentation Backlog

- Convert the long session-history block currently inside root `AGENTS.md` into a reviewed historical archive when there is a dedicated docs-cleanup task.
- Add or update active task briefs under `docs/active/` only when a task is truly live.
- Keep owner workspace docs synchronized with completed implementation evidence.
- Run the reviewed markdown archive/marking pass defined by `docs/architecture/markdown-freshness-authority-audit.md`: resolve the `AGENT-COMPLIANCE-CHECKLIST.md` naming collision, extract root session logs into a historical archive, and add in-place status headers to confirmed superseded/stale candidates (grep gates/probes for doc references before any change).

