# Theme Architecture V1 Completion — Prompt 5/5

**Date**: 2026-06-05
**Status**: Theme Architecture V1 cleanup complete across all 5 prompts.

---

## Before/After Summary

| Aspect | Before V1 | After V1 |
|---|---|---|
| Token source | Single interleaved `foundation.css` | Two layers: `foundation.css` (primitives) + `semantic/semantic.css` (semantic) |
| Component selectors | Mixed into theme source files | Extracted to owner CSS (Shell, Manufacturing, Coverage) |
| Dead CSS | `.sc-card` in theme sources | Removed |
| Runtime artifact | Tracked in git, manually maintained | untracked `.gitignore`, auto-generated, marked `GENERATED FILE` |
| Architecture guardrails | None for theme integrity | `check_theme_source_integrity.sh` wired into aggregate gates |
| Total gates | 28 | 29 |

---

## Selector Extraction Summary (Prompts 1-3)

### Prompt 1/2 — Shell structural selectors
- Extracted topbar, sidebar, card, button, input, select, textarea, table, and workflow selectors from `resources/themes/dark.css` and `resources/themes/liquid-glass.css` into `apps/Shell/styles/shell.css`.
- Shared theme variant selectors (dark backdrop-filter/border-color, liquid-glass backdrop-filter/border-color) now live in Shell CSS.

### Prompt 3 — Remaining non-Shell selectors
- **Manufacturing/Coverage** (`.coverage-kpi`, `.coverage-trend-wrap`) → `apps/Manufacturing/modules/Coverage/styles.css`
- **Manufacturing** (`.mfg-subgroup-card`) → `apps/Manufacturing/styles/manufacturing.css`
- **Base plugin** (`.menu .group`, `.ai-kpi`, `.ai-item`, `.ai-details`, `.ai-table-wrap`) → `apps/Shell/styles/shell.css` (Base plugin has no CSS loading mechanism)
- **`.sc-card`** — dead CSS, removed (zero PHP template matches)
- **`.app-quicklink-card`** — verified shared Shell component, retained in Shell CSS

### Result
All 5 theme source files (`foundation.css`, `light.css`, `dark.css`, `liquid-glass.css`, `paper.css`) are **tokens-only** — zero component selectors.

---

## Foundation/Semantic Separation Summary (Prompt 4)

### Classification
All 161 tokens in `resources/themes/foundation.css` were classified:

| Layer | Count | Examples |
|---|---|---|
| Foundation primitives | 29 | `--space-*`, `--radius-*`, `--glass-blur-*`, `--depth`, `--transition`, `--font-sans`, `--topbar-height`, `--safe-area-*`, `--focus-ring`, `--select-scheme` |
| Semantic tokens | 132 | `--bg`, `--text`, `--panel`, `--accent`, `--glass-*`, `--notif-*`, `--style-*`, `--control-*`, `--tone-*`, `--color-*`, `--card-*`, `--chrome-*`, `--icon-chip-*` |

### Files
- `resources/themes/foundation.css` — 29 tokens (stripped from 161)
- `resources/themes/semantic/semantic.css` — 132 tokens (new file)
- `resources/themes/theme-manifest.json` — 8 sources (+ `semantic` entry)

### Validation
- Token value parity: 161/161 vs git HEAD — zero mismatches
- No token renames, value changes, or new concepts introduced

---

## Runtime Artifact Rule

`public/assets/theme.css` is compiled output only:

- Marked with `/* GENERATED FILE: public/assets/theme.css */` header
- Untracked in git (in `.gitignore`)
- Auto-regenerated by `public/index.php` on stale mtime
- Compiled by `scripts/assets/compile_theme_sources.php` from manifest-declared source files
- Contains zero component selectors — only `:root`, `[data-theme="*"]`, `[data-color-style="*"]` token declarations

---

## Regression Guardrails (Prompt 5)

New diagnostic: `scripts/architecture/check_theme_source_integrity.sh`

### Invariants checked
1. Required theme source files exist (7 files)
2. No component selectors in theme source files
3. `foundation.css` contains all 29 approved primitive tokens
4. `foundation.css` contains no disallowed semantic tokens
5. `semantic/semantic.css` exists with semantic token categories
6. `public/assets/theme.css` is generated output (`GENERATED FILE` marker)
7. Known owner selectors (`.coverage-kpi`, `.mfg-subgroup-card`, `.sc-card`, etc.) are not reintroduced into theme sources
8. Manifest source order is `foundation < semantic < light` for correct cascade

### Integration
- Wired into `run_architecture_gates.sh` (28 → 29 gates)
- Gate-runner contract updated with ordering rationale
- Architecture gate coverage index updated with full coverage map

---

## Validation Evidence

| Check | Result |
|---|---|
| `check_theme_source_integrity.sh` direct run | PASS (0 failures, 0 warnings) |
| `run_architecture_gates.sh` | PASS (29/29 gates) |
| `check_deployment_readiness.sh` | PASS (all gates) |
| `git diff --check` | PASS |
| PHP lint / bash syntax | PASS |
| Browser smoke (admin/studio/mfg) | 11/11 PASS |

---

## Remaining Known Debt

1. **Variant set documentation** — Variants (`light-paper`, `light-liquid-glass`, `dark-paper`, `dark-liquid-glass`) are not yet declared as an explicit named set. Current files work but the set is implicit.
2. **Navy/Obsidian disabled compatibility** — Kept as `enabled: false` in manifest until locked `/app` defaults no longer reference `system-obsidian`.
3. **ThemePreferenceService style discovery** — Currently scans `resources/themes/**` plus manifest. Future slices should normalize to manifest-only discovery.
4. **Compiler subdirectory support** — Currently reads flat files via manifest. Future slices may add structured subdirectory file support.
5. **Studio editing target** — CTE/Theme tooling still targets runtime artifact; source-resource editing is Phase 4 work (not yet started).

These are non-blocking for V1 completion and tracked in `TODO_THEME_FIX.md` Phases 4-5.

---

## Diff Summary (All 5 Prompts, Uncommitted)

```
 AGENTS.md                                      |  73 +++
 TODO_THEME_FIX.md                              |  43 +-
 audit/theme-css-non-shell-extraction-*.md       | new
 audit/theme-token-layer-separation-*.md         | new
 audit/theme-architecture-v1-completion-*.md     | new
 apps/Manufacturing/modules/Coverage/styles.css  |  15 +
 apps/Manufacturing/styles/manufacturing.css     |  13 +
 apps/Shell/styles/shell.css                     | 138 +
 resources/themes/dark.css                       |  38 -
 resources/themes/foundation.css                 | 134 --
 resources/themes/semantic/semantic.css          | new (132 tokens)
 resources/themes/light.css                      |  52 -
 resources/themes/liquid-glass.css               |  52 -
 resources/themes/theme-manifest.json            |  18 +-
 scripts/architecture/check_theme_source_*.sh     | new (diagnostic)
 scripts/architecture/run_architecture_gates.sh  |   4 +-
 scripts/architecture/gate-runner-contract.md    |   4 +-
 docs/architecture/architecture-gate-*.md         |  33 +-
 AGENT-COMPLIANCE-CHECKLIST.md                  |  54 +
```

## Commit Message

```
chore(theme): lock theme architecture v1 guardrails

- add check_theme_source_integrity.sh diagnostic
- wire into aggregate architecture gates (28→29)
- update gate-runner-contract and coverage index
- mark all 5 Theme Architecture V1 prompts complete
```
