# Style Resources

Placeholder home for future Shell style resource metadata.

Resources here are not active runtime data. Future active/default style truth belongs to Platform/System, while apps/modules own scoped CSS and consume shared Shell sockets.
